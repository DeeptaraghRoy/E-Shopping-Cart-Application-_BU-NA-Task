server:
  port: 9296
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DeeptaraghRoy/ECommerce_Config_Server.git
          clone-on-start: true
